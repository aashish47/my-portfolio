import { Name } from "@/constants/constants";
import { Metadata } from "next";

export const metadata: Metadata = {
    title: `Uses | ${Name}`,
};

const Uses = () => {
    return <main></main>;
};

export default Uses;
