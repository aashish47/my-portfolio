import { Balancer } from "react-wrap-balancer";

const NotFound = () => {
    return (
        <main className=" text-2xl flex justify-center font-bold  ">
            <Balancer> 404 | The page you are looking for does not exist !!</Balancer>
        </main>
    );
};

export default NotFound;
